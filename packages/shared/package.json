{
  "name": "@instrumentality/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared parsers, types, and prompt templates for the kb-mcp sync dashboard.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build:catalog": "node scripts/build-tool-catalog.cjs",
    "build": "npm run build:catalog && tsc",
    "watch": "npm run build:catalog && tsc --watch",
    "test": "node --test --import tsx test/contract.test.ts test/prompts.test.ts test/standards-parser.test.ts test/grouping.test.ts test/drift-log.test.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
