{
  "name": "@instrumentality/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared parsers, types, and prompt templates for the kb-mcp sync dashboard.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "node --test --import tsx test/contract.test.ts test/prompts.test.ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
