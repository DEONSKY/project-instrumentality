node_modules/
dist/

# Transient capture dir for knowledge/_mcp/tests/fixtures/verify.sh.
# Wiped and regenerated on every run; diffed against baseline/.
knowledge/_mcp/tests/fixtures/current/

# Transient kb_import resume state (holds full source-doc text mid-run).
knowledge/sync/.import-sessions/

# Transient kb_import image staging (pre-approve; relocated to assets/ on approve).
knowledge/assets/imports/.staging/
